<?xml version="1.0" encoding="utf-8"?>
<layer-list
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:fromDegrees="270"
    android:pivotX="50%"
    android:pivotY="50%"
    android:toDegrees="270">

    <item android:id="@+id/secondaryProgress">

        <shape
            android:shape="ring"
            android:innerRadiusRatio="3"
            android:thicknessRatio="12.0"
            android:useLevel="false">

            <gradient
                android:startColor="#454c5f"
                android:endColor="#454c5f"
                android:centerColor="#454c5f"
                android:type="sweep"/>

        </shape>
    </item>

    <item android:id="@+id/progress">

        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">

            <shape
                android:shape="ring"
                android:innerRadiusRatio="3"
                android:thicknessRatio="12.0"
                android:useLevel="true">

                <corners android:radius="10dip"/>

                <gradient
                    android:centerColor="@color/for_round"
                    android:endColor="@color/for_round"
                    android:startColor="@color/for_round"
                    android:type="sweep"/>
            </shape>
        </rotate>
    </item>

</layer-list>